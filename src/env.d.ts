/**
 * src/env.d.ts
 *
 * Type definitions for environment bindings and secrets
 * These are not auto-generated by wrangler types and must be manually defined
 * Extends the auto-generated Env interface from worker-configuration.d.ts
 *
 * Top-level declarations:
 * - Env: Extends Env in cloudflare:workers module with secrets and bindings
 */

declare module "cloudflare:workers" {
  interface Env extends CloudflareBindings {
    AUTHORIZED_SENDERS: string;
    HYPERDRIVE: Hyperdrive;
    CF_AIG_TOKEN: string;
  }
}

declare global {
  interface Env extends CloudflareBindings {
    AUTHORIZED_SENDERS: string;
    HYPERDRIVE: Hyperdrive;
    CF_AIG_TOKEN: string;
  }
}

export {};
